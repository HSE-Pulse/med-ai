apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "medai-service.fullname" . }}-env
  labels:
    {{- include "medai-service.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
