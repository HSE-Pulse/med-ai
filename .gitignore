# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg
dist/
build/
.eggs/

# Environment
.env
.venv/
venv/

# Data & Models (too large for git)
datasets/
models/
# ...but app_NN/backend/models/ holds source, not artifacts: training
# scripts, model wrappers and evaluation harnesses. The bare "models/"
# pattern matched those directories too, so that code was silently
# untracked — app_03's train.py had never been committed. Weights are
# still excluded by the *.pt / *.joblib / *.npz rules below.
!app_*/backend/models/
*.parquet
*.npz
*.pt
*.joblib
*.pkl
*.rar

# Node
node_modules/
dashboard/dist/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Logs
*.log
runs/
ray_results/

# Linux-side runtime artefacts (Windows→Linux migration, 2026-05)
.mongo-data/
.mongo-data.corrupt/
.mongo-dump/
.tmp/
.tmp_*/
.tmp_post_fix_audit.py
.claude/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Windows debug artefacts that drifted in via the rsync from D: drive
*.stackdump
du.exe.stackdump

# Open-source hygiene: never re-add patient-view screenshots, office binaries,
# or confidential business docs to a public fork.
dashboard/screenshots/
*.docx
*.pptx
*.xlsx
docs/BRD.md

# Confidential / working dirs and artifacts — never publish to a public fork
personal/
pitch/
.tmp_*
*.tsbuildinfo
app_04b_oncology_clone.zip
app_04b_oncology_clone/
